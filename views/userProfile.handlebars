<div class = "paddingDiv">
  <p><strong>{{welcome}}<strong></p>
  <img src="{{user.profile_picture}}" height="210" width="210"/>
  <h2>User Info</h2>
  <form class = "formClass" action="/userProfile" method="post">
    <input type="hidden" name="_id" value="{{user._id}}">
    Name: <input type="text" name="fname" value="" placeholder="First Name">
    Last: <input type="text" name="lname" value="" placeholder="Last Name"><br>
    Bio:<br> <textarea rows="8" cols="60" type="text" name="bio" value="{{user.bio}}" placeholder="bio" ></textarea><br><br>
   <input type="submit" value="Submit">
  <form>
  {{#if success}}
  <p>
    {{success}}
  </p>
  {{/if}}
</div>
